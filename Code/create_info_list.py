from Code.CONSTANTS import (
    FULL_INFO,
    TARGET_FOLDERS,
    MASKS,
    NAMES_AND_RULES,
    ONLY_NAMES,
)


def get_info_list(mode, order):
    info_list = []
    for x in range(15):
        info_list.append(x)
    return info_list
